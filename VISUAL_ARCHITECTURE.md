# 🎨 Community Collaboration Platform - Visual Guide

## 📐 System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                          USER INTERFACE                             │
│                     (public/group.html)                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐            │
│  │ Video Call   │  │   To-Do      │  │    Notes     │            │
│  │   Button     │  │    Tab       │  │     Tab      │            │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘            │
│         │                  │                  │                     │
│         ▼                  ▼                  ▼                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐            │
│  │GroupCallMgr  │  │  TodoBoard   │  │ NotesEditor  │            │
│  │ group-call.js│  │todo-board.js │  │notes-edit.js │            │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘            │
│         │                  │                  │                     │
│         └──────────────────┴──────────────────┘                     │
│                            │                                        │
│                     ┌──────▼───────┐                               │
│                     │  GroupMain   │                               │
│                     │group-main.js │                               │
│                     └──────┬───────┘                               │
│                            │                                        │
└────────────────────────────┼────────────────────────────────────────┘
                             │
                             │ Socket.IO + REST API
                             │
┌────────────────────────────▼────────────────────────────────────────┐
│                     NODE.JS SERVER                                  │
│                       (server.js)                                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │                   Socket.IO Events                           │ │
│  │  • group-call:join, group-call:signal, group-call:leave     │ │
│  │  • note:remote-update, note:user-editing, note:user-left    │ │
│  │  • group:message, group:join, group:leave                   │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │                REST API Routes                               │ │
│  │  routes/community-groups.js                                  │ │
│  │  • Tasks CRUD      • Notes CRUD                              │ │
│  │  • Files CRUD      • Members CRUD                            │ │
│  │  • Links CRUD      • Version History                         │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                     │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       SQLITE DATABASE                               │
│                      (database/innovate.db)                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  • communities              • community_group_tasks                │
│  • community_members        • community_group_notes                │
│  • community_groups         • community_group_note_versions        │
│  • community_group_members  • community_group_files                │
│  • community_group_posts    • community_group_links                │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 Feature Flow Diagrams

### 1. Video Call Flow

```
User clicks "Video Call" button
         │
         ▼
GroupCallManager.startCall(false)
         │
         ├─── Get local camera/mic stream
         │    └─ navigator.mediaDevices.getUserMedia()
         │
         ├─── Emit Socket.IO event
         │    └─ socket.emit('group-call:join', {groupId, userId, displayName})
         │
         ├─── Receive list of existing participants
         │    └─ socket.on('group-call:peers', callback)
         │
         ├─── Create peer connections for each participant
         │    └─ For each peer: createPeerConnection(peerId)
         │          │
         │          ├─ Create RTCPeerConnection
         │          ├─ Add local tracks (video/audio)
         │          ├─ Create offer
         │          ├─ Send offer via socket
         │          └─ Receive answer via socket
         │
         └─── Display video grid
              └─ Show all participant videos + own video
```

---

### 2. AI To-Do Creation Flow

```
User clicks "Create Task"
         │
         ▼
TodoBoard.showCreateTaskModal()
         │
         ├─── Opens 3-tab modal
         │    ├─ Text Tab
         │    ├─ Image Tab
         │    └─ Voice Tab
         │
         ▼
User selects "Image Tab"
         │
         ├─── User uploads photo
         │    └─ <input type="file" accept="image/*">
         │
         ├─── Send to ML service
         │    └─ POST /api/ml/tasks/from-image
         │         Body: { image: base64_data, groupId: 123 }
         │
         ├─── ML service analyzes (OCR)
         │    └─ Extracts tasks from image
         │         Returns: [
         │           {title: "Task 1", description: "..."},
         │           {title: "Task 2", description: "..."}
         │         ]
         │
         ├─── Save to database
         │    └─ POST /api/community-groups/123/tasks
         │         For each extracted task:
         │           INSERT INTO community_group_tasks (...)
         │
         └─── Refresh Kanban board
              └─ todoBoard.loadTasks()
```

---

### 3. Collaborative Notes Flow

```
User A opens note          User B opens same note
         │                          │
         ├──────────────────────────┤
         │                          │
         ▼                          ▼
NotesEditor.loadNote(noteId)   NotesEditor.loadNote(noteId)
         │                          │
         ├─── Load from API ────────┤
         │    GET /api/community-groups/123/notes/456
         │                          │
         ├─── Initialize Quill.js ──┤
         │    new Quill(container)  │
         │                          │
         ├─── Join Socket.IO room ──┤
         │    socket.emit('note:join', {noteId, userId})
         │                          │
         ▼                          ▼
User A types "Hello"       User B sees "Hello" appear
         │                          │
         ├─── Quill detects change  │
         │    editor.on('text-change')
         │                          │
         ├─── Emit to socket ───────┼─► socket.on('note:remote-update')
         │    socket.emit(           │   │
         │      'note:remote-update',│   │
         │      {noteId, delta, userId} │
         │    )                      │   │
         │                          │   │
         │                          │   ├─ Apply delta to editor
         │                          │   │  editor.updateContents(delta)
         │                          │   │
         │                          │   └─ Show typing indicator
         │                          │      "User A is editing"
         │                          │
         ├─── Auto-save after 2s ───┤
         │    PUT /api/community-groups/123/notes/456
         │    Body: { content_md: "..." }
         │                          │
         └─── Save version ─────────┤
              INSERT INTO community_group_note_versions (...)
```

---

## 🗂️ File Structure Map

```
/workspaces/Innovate-Hub/
│
├── 📄 server.js                      # Main server entry
│   └─ Express app setup
│   └─ Socket.IO server
│   └─ WebRTC signaling events
│
├── 📁 routes/
│   ├── communities.js                # Community CRUD
│   └── community-groups.js           # ✨ MAIN API FILE
│       ├─ Groups CRUD
│       ├─ Tasks CRUD (To-Do)
│       ├─ Notes CRUD
│       ├─ Version History
│       ├─ Files CRUD
│       └─ Links CRUD
│
├── 📁 config/
│   └── database.js                   # SQLite schema
│       └─ All table definitions
│
├── 📁 public/
│   │
│   ├── 📄 group.html                 # Main group page UI
│   │   ├─ Call buttons (📹 📞)
│   │   ├─ Tab navigation
│   │   ├─ Call container
│   │   ├─ To-Do container
│   │   └─ Notes container
│   │
│   ├── 📁 js/
│   │   ├── group-main.js             # ✨ Main controller
│   │   │   ├─ Initializes all managers
│   │   │   ├─ Chat handling
│   │   │   ├─ File uploads
│   │   │   └─ Tab switching
│   │   │
│   │   ├── group-call.js             # ✨ WebRTC manager
│   │   │   ├─ GroupCallManager class
│   │   │   ├─ startCall()
│   │   │   ├─ toggleMic()
│   │   │   ├─ toggleCamera()
│   │   │   ├─ shareScreen()
│   │   │   └─ endCall()
│   │   │
│   │   ├── todo-board.js             # ✨ To-Do board
│   │   │   ├─ TodoBoard class
│   │   │   ├─ renderBoard() - Kanban
│   │   │   ├─ showCreateTaskModal() - 3 tabs
│   │   │   ├─ analyzeImageForTasks() - OCR
│   │   │   └─ analyzeVoiceForTasks() - STT
│   │   │
│   │   └── notes-editor.js           # ✨ Notes editor
│   │       ├─ NotesEditor class
│   │       ├─ initializeEditor() - Quill
│   │       ├─ setupSocketListeners() - Real-time
│   │       ├─ saveNote() - Auto-save
│   │       └─ showVersionHistory()
│   │
│   └── 📁 css/
│       ├── instagram.css             # Main theme
│       └── group-call.css            # ✨ Call UI styles
│
├── 📁 database/
│   └── innovate.db                   # SQLite database
│
└── 📁 ml-service/
    └── app.py                        # Python ML service
        ├─ /api/ml/tasks/from-image   # OCR endpoint
        └─ /api/ml/tasks/from-voice   # STT endpoint
```

---

## 🎨 UI Component Hierarchy

### Group Page Layout:

```
┌─────────────────────────────────────────────────────────────┐
│ 🏠 Innovate Hub                    📹 Video  📞 Audio  👤   │ ← Header
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Tabs: Feed | To-Do | Notes | Links | Files | Members│   │ ← Tabs
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                       │   │
│  │                  MAIN CONTENT AREA                   │   │
│  │                                                       │   │
│  │  ┌──────────────────────────────────────────────┐   │   │
│  │  │                                              │   │   │
│  │  │   Feed: Chat messages                        │   │   │
│  │  │   To-Do: Kanban board                        │   │   │
│  │  │   Notes: Rich text editor                    │   │   │
│  │  │   Files: Grid/List view                      │   │   │
│  │  │                                              │   │   │
│  │  └──────────────────────────────────────────────┘   │   │
│  │                                                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘

When Video Call Started:
┌─────────────────────────────────────────────────────────────┐
│ ← Back                                                      │ ← Call Header
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐           │
│  │            │  │            │  │            │           │
│  │  User 1    │  │  User 2    │  │  User 3    │           │ ← Video Grid
│  │   (You)    │  │            │  │            │           │
│  └────────────┘  └────────────┘  └────────────┘           │
│                                                             │
│  ┌────────────────────────────────────────────────────┐    │
│  │  🎤    📹    🖥️    ❌                              │    │ ← Controls
│  │  Mic  Cam  Share  End                             │    │
│  └────────────────────────────────────────────────────┘    │
│                                                             │
│  Participants:                                              │
│  • User 1 (You)                                             │ ← Participants
│  • User 2                                                   │   List
│  • User 3                                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘

To-Do Board (Kanban):
┌─────────────────────────────────────────────────────────────┐
│ To-Do Board                           ➕ Create Task        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────┐   ┌───────────┐   ┌──────────┐               │
│  │ TO-DO   │   │IN PROGRESS│   │   DONE   │               │
│  ├─────────┤   ├───────────┤   ├──────────┤               │
│  │┌───────┐│   │┌─────────┐│   │┌────────┐│               │
│  ││Task 1 ││   ││Task 4   ││   ││Task 7  ││               │
│  ││High   ││   ││Medium   ││   ││Low     ││               │
│  │└───────┘│   │└─────────┘│   │└────────┘│               │
│  │┌───────┐│   │┌─────────┐│   │          │               │
│  ││Task 2 ││   ││Task 5   ││   │          │               │
│  ││Medium ││   ││High     ││   │          │               │
│  │└───────┘│   │└─────────┘│   │          │               │
│  │┌───────┐│   │┌─────────┐│   │          │               │
│  ││Task 3 ││   ││Task 6   ││   │          │               │
│  ││Low    ││   ││Medium   ││   │          │               │
│  │└───────┘│   │└─────────┘│   │          │               │
│  └─────────┘   └───────────┘   └──────────┘               │
│                                                             │
└─────────────────────────────────────────────────────────────┘

Create Task Modal:
┌─────────────────────────────────────────────────────────────┐
│ Create New Task                                         ✕   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────┬────────────┬────────────┐                     │
│  │  Text   │   Image    │   Voice    │  ← Tabs             │
│  └─────────┴────────────┴────────────┘                     │
│                                                             │
│  Text Tab:                                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Task Title: ___________________________________     │   │
│  │ Description: ________________________________       │   │
│  │ Priority: [High ▼]                                 │   │
│  │ Due Date: [2024-01-20]                             │   │
│  │ Assignees: [Select members...]                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Image Tab:                                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  📷 Upload Image                                    │   │
│  │                                                      │   │
│  │  [Drag & drop or click to upload]                   │   │
│  │                                                      │   │
│  │  AI will analyze the image and extract tasks        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Voice Tab:                                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  🎤 Record Voice                                     │   │
│  │                                                      │   │
│  │  [⏺️ Start Recording] [⏹️ Stop]                       │   │
│  │                                                      │   │
│  │  AI will transcribe and extract tasks                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│                    [Cancel]  [Create Task]                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘

Notes Editor:
┌─────────────────────────────────────────────────────────────┐
│ My Notes                           ➕ New  💾 Save  📜 History│
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📝 Meeting Notes - Jan 12                           │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ Last edited by User A • 2 minutes ago              │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ B I U S H1 H2 [≡] 🔗 {} ""                         │   │ ← Toolbar
│  ├─────────────────────────────────────────────────────┤   │
│  │                                                      │   │
│  │ # Meeting Agenda                                     │   │
│  │                                                      │   │
│  │ 1. Project updates                                   │   │ ← Editor
│  │ 2. New feature discussion                            │   │   (Quill.js)
│  │ 3. Sprint planning                                   │   │
│  │                                                      │   │
│  │ ## Action Items                                      │   │
│  │ - [ ] User A: Complete API docs                      │   │
│  │ - [ ] User B: Review PR #123                         │   │
│  │                                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  👤 User A is editing... (live indicator)                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Sequences

### 1. Task Creation (Text Input):
```
Browser → POST /api/community-groups/123/tasks
          {title, description, priority, due_date}
          ↓
Server  → Validate auth token
          ↓
        → Check user is group member
          ↓
        → INSERT INTO community_group_tasks (...)
          ↓
        → Return task object
          ↓
Browser ← Receive task
          ↓
        → Add to Kanban board
          ↓
        → Render in "To-Do" column
```

### 2. Task Creation (Image Input):
```
Browser → Upload image
          ↓
        → Convert to base64
          ↓
        → POST /api/ml/tasks/from-image
          {image: base64, groupId}
          ↓
ML Service → Decode image
          ↓
           → Run OCR (pytesseract)
          ↓
           → Extract text
          ↓
           → Parse for tasks
          ↓
           → Return [{title, description}, ...]
          ↓
Browser ← Receive extracted tasks
          ↓
        → For each task:
          │
          ├─ POST /api/community-groups/123/tasks
          │  {title, description, source_type: 'image'}
          │
          └─ Add to Kanban board
```

### 3. Real-Time Note Editing:
```
User A Types "Hello"
     │
     ▼
Quill detects change
     │
     ├─ Get delta (change object)
     │  {ops: [{insert: "Hello"}]}
     │
     ├─ Emit to Socket.IO
     │  socket.emit('note:remote-update', {
     │    noteId: 123,
     │    userId: 1,
     │    delta: {...}
     │  })
     │
     ▼
Server receives event
     │
     ├─ Broadcast to all in room
     │  io.to(`note_${noteId}`).emit('note:remote-update', data)
     │
     ▼
User B receives event
     │
     ├─ Check if from different user
     │  if (data.userId !== myUserId)
     │
     ├─ Apply delta to editor
     │  editor.updateContents(delta, 'silent')
     │
     └─ Update UI
        "User A is editing..."
```

---

## 📊 Class Diagrams

### GroupCallManager:
```
┌────────────────────────────────────────┐
│      GroupCallManager                  │
├────────────────────────────────────────┤
│ - groupId: number                      │
│ - socket: Socket                       │
│ - localStream: MediaStream             │
│ - peerConnections: Map<string, PC>    │
│ - isCallActive: boolean                │
│ - isMuted: boolean                     │
│ - isCameraOff: boolean                 │
├────────────────────────────────────────┤
│ + startCall(audioOnly)                 │
│ + endCall()                            │
│ + toggleMic()                          │
│ + toggleCamera()                       │
│ + shareScreen()                        │
│ + stopScreenShare()                    │
│ - createPeerConnection(peerId)         │
│ - setupSocketListeners()               │
│ - addParticipantVideo(peerId, stream)  │
│ - removeParticipantVideo(peerId)       │
└────────────────────────────────────────┘
```

### TodoBoard:
```
┌────────────────────────────────────────┐
│         TodoBoard                      │
├────────────────────────────────────────┤
│ - groupId: number                      │
│ - tasks: Array<Task>                   │
│ - currentModal: HTMLElement            │
├────────────────────────────────────────┤
│ + loadTasks()                          │
│ + showCreateTaskModal()                │
│ + createTaskFromText(data)             │
│ + analyzeImageForTasks(file)           │
│ + analyzeVoiceForTasks(audioBlob)      │
│ + updateTaskStatus(taskId, status)     │
│ + deleteTask(taskId)                   │
│ - renderBoard()                        │
│ - renderTask(task)                     │
│ - setupDragAndDrop()                   │
└────────────────────────────────────────┘
```

### NotesEditor:
```
┌────────────────────────────────────────┐
│        NotesEditor                     │
├────────────────────────────────────────┤
│ - groupId: number                      │
│ - socket: Socket                       │
│ - editor: Quill                        │
│ - currentNoteId: number                │
│ - notes: Array<Note>                   │
│ - saveTimeout: Timer                   │
├────────────────────────────────────────┤
│ + loadNotes()                          │
│ + createNote()                         │
│ + openNote(noteId)                     │
│ + saveNote()                           │
│ + deleteNote(noteId)                   │
│ + showVersionHistory()                 │
│ + restoreVersion(versionId)            │
│ - initializeEditor()                   │
│ - setupSocketListeners()               │
│ - handleRemoteUpdate(delta)            │
└────────────────────────────────────────┘
```

---

## 🎉 Visual Summary

```
╔═══════════════════════════════════════════════════════════════╗
║      COMMUNITY COLLABORATION PLATFORM - COMPLETE              ║
╠═══════════════════════════════════════════════════════════════╣
║                                                               ║
║  ✅ Voice/Video Calls         [GroupCallManager]             ║
║     • WebRTC peer-to-peer                                    ║
║     • Screen sharing                                         ║
║     • Mic/camera controls                                    ║
║                                                               ║
║  ✅ AI-Powered To-Do          [TodoBoard]                    ║
║     • Kanban board                                           ║
║     • Text/Image/Voice input                                 ║
║     • Drag & drop                                            ║
║                                                               ║
║  ✅ Collaborative Notes       [NotesEditor]                  ║
║     • Rich text editing                                      ║
║     • Real-time sync                                         ║
║     • Version history                                        ║
║                                                               ║
║  ✅ Integration               [GroupMain]                    ║
║     • Chat messaging                                         ║
║     • File management                                        ║
║     • Member management                                      ║
║                                                               ║
╠═══════════════════════════════════════════════════════════════╣
║                                                               ║
║  🚀 Status: 100% COMPLETE - READY FOR USE                    ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝
```

---

*Diagrams created: January 12, 2026*  
*Visual guide for complete implementation*
